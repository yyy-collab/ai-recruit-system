$kind: http-request
url: "{{baseUrl}}/hr/message/list?pageNum=1&pageSize=10&status=1"
method: GET
headers:
  Accept: application/json
queryParams:
  pageNum: "1"
  pageSize: "10"
  status: "1"
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/HR获取消息通知列表.resources/examples
order: 1000
