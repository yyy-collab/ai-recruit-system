$kind: http-request
url: "{{baseUrl}}/hr/statistics"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/HR招聘数据统计.resources/examples
order: 1000
