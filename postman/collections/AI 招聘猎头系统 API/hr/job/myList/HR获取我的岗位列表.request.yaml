$kind: http-request
url: "{{baseUrl}}/hr/job/myList?pageNum=1&pageSize=10&status=1&job_name=string"
method: GET
headers:
  Accept: application/json
queryParams:
  pageNum: "1"
  pageSize: "10"
  status: "1"
  job_name: string
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/HR获取我的岗位列表.resources/examples
order: 1000
