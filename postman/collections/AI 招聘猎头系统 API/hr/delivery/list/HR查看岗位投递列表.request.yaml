$kind: http-request
url: "{{baseUrl}}/hr/delivery/list?job_id=5728&pageNum=1&pageSize=10&status=2&sort=match_score_desc"
method: GET
headers:
  Accept: application/json
queryParams:
  job_id: "5728"
  pageNum: "1"
  pageSize: "10"
  status: "2"
  sort: match_score_desc
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/HR查看岗位投递列表.resources/examples
order: 1000
