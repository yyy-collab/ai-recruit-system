$kind: collection
description: 基于 HanLP + TF-IDF + 余弦相似度的智能招聘系统
variables:
  baseUrl: http://localhost:8080
auth:
  - id: 08703576-5c5d-4c3d-9ea5-ad4c66ca3331
    type: bearer
    name: bearer auth
    credentials:
      token: "{{bearerToken}}"
