$kind: http-request
url: "{{baseUrl}}/seeker/job/detail?job_id=5728"
method: GET
headers:
  Accept: application/json
queryParams:
  job_id: "5728"
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/求职者查看岗位详情.resources/examples
order: 1000
