$kind: http-example
request:
  url: "{{baseUrl}}/seeker/job/list?pageNum=1&pageSize=10&job_name=string&sort=salary_desc"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    pageNum: "1"
    pageSize: "10"
    job_name: string
    sort: salary_desc
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "code": 7933,
        "msg": "string",
        "data": {}
      }
order: 1000
