$kind: http-request
url: "{{baseUrl}}/seeker/userInfo"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/获取当前求职者信息.resources/examples
order: 1000
