$kind: http-request
url: "{{baseUrl}}/seeker/resetPwd"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "username": "string",
      "email": "NvyDQ3L6RHe@e.ito",
      "code": "string",
      "new_pwd": "string",
      "re_pwd": "string"
    }
examples: ./.resources/求职者重置密码（未登录）.resources/examples
order: 1000
