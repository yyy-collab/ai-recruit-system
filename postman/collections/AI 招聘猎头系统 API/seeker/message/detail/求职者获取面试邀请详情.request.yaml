$kind: http-request
url: "{{baseUrl}}/seeker/message/detail?message_id=5728"
method: GET
headers:
  Accept: application/json
queryParams:
  message_id: "5728"
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/求职者获取面试邀请详情.resources/examples
order: 1000
