$kind: http-request
url: "{{baseUrl}}/resume/preview?resume_id=4218"
method: GET
headers:
  Accept: application/json
queryParams:
  resume_id: "4218"
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/简历在线预览.resources/examples
order: 1000
