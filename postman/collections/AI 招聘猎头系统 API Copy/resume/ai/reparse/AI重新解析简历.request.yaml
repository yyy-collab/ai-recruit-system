$kind: http-request
url: "{{baseUrl}}/resume/ai/reparse"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "resume_id": 7605
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/AI重新解析简历.resources/examples
order: 1000
