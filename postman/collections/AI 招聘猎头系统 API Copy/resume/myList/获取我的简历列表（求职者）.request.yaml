$kind: http-request
url: "{{baseUrl}}/resume/myList"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/获取我的简历列表（求职者）.resources/examples
order: 1000
