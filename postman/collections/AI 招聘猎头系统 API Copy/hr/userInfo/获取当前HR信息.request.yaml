$kind: http-request
url: "{{baseUrl}}/hr/userInfo"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/获取当前HR信息.resources/examples
order: 1000
