$kind: http-request
url: "{{baseUrl}}/seeker/delivery/myList?pageNum=1&pageSize=10&status=3"
method: GET
headers:
  Accept: application/json
queryParams:
  pageNum: "1"
  pageSize: "10"
  status: "3"
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/求职者查看我的投递记录.resources/examples
order: 1000
