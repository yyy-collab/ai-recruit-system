$kind: http-request
url: "{{baseUrl}}/seeker/delivery/add"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "job_id": 7213
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/求职者投递岗位.resources/examples
order: 1000
