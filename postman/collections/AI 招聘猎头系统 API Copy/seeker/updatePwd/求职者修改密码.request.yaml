$kind: http-request
url: "{{baseUrl}}/seeker/updatePwd"
method: PATCH
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "old_pwd": "string",
      "new_pwd": "5umZfqPrcnO",
      "re_pwd": "string"
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/求职者修改密码.resources/examples
order: 1000
