$kind: http-request
url: "{{baseUrl}}/seeker/message/list?pageNum=1&pageSize=10&status=2"
method: GET
headers:
  Accept: application/json
queryParams:
  pageNum: "1"
  pageSize: "10"
  status: "2"
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/求职者获取消息通知列表.resources/examples
order: 1000
