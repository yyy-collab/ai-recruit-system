$kind: http-request
url: "{{baseUrl}}/seeker/interview/handle"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "message_id": 945,
      "status": 1,
      "reject_reason": "string"
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/求职者处理面试邀请.resources/examples
order: 1000
