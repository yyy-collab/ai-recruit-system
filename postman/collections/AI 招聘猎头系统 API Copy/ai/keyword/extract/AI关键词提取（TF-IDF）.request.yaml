$kind: http-request
url: "{{baseUrl}}/ai/keyword/extract"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "type": "text",
      "content": "string",
      "resume_id": 4581,
      "job_id": 2126,
      "limit": 10
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/AI关键词提取（TF-IDF）.resources/examples
order: 1000
